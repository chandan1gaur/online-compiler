# robots.txt - Online Compiler SEO Configuration (cleaned)
# Keep rules minimal and standard-compliant. Remove nonstandard directives.
User-agent: *
Allow: /
Disallow: /.next/
Disallow: /node_modules/

Sitemap: https://www.codecompileronline.com/sitemap.xml

# Note: `Priority:` is not a robots directive; sitemap.xml may include priority tags instead.
# `Crawl-delay` is supported by some crawlers but ignored by Google; add only if needed.
